/*
#include <iostream>

using std::cout;
using std::cin;
using std::endl;

int main() {
	//Primzahl-Test
	bool is_prime = true;
	int prüfzahl;
	cout << "Bitte gib eine Zahl ein: ";
	cin >> prüfzahl;

	for (int i = 2; i < prüfzahl; i++)
	{
		if (prüfzahl%i == 0)
		{
			is_prime = false;
			break;
		}
	}

	if (is_prime == false)
	{
		cout << "Die Zahl " << prüfzahl << " ist keine Primzahl." << endl;
	}
	else 
	{
		cout << "Die Zahl " << prüfzahl << " ist eine Primzahl." << endl;
	}

	return 0;
}*/